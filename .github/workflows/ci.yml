name: Testing
on: push

jobs:
  test_project:
    run-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Run Django unit tests
      run:  |
        pip3 install --user django
        python manahe.py test